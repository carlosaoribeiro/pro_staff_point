import 'package:flutter/material.dart';

class VacationsScreen extends StatelessWidget {
  const VacationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Férias')),
    );
  }
}
